# Admin Express API env (copy to .env.local)
PORT=5001
CORS_ORIGIN=http://localhost:3000

# Supabase
SUPABASE_URL=your_supabase_url
SUPABASE_ANON_KEY=your_anon_key
# Needed for privileged admin operations (approvals, etc.)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Optional: explicit path for env file
# ENV_PATH=.env.local
